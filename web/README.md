# Web app (`web/`)

This directory contains the **Channel Explorer** frontend. See the **[root README](../README.md)** for product context and features.

**Quick start:** `npm install` → `npm run dev`.
